---
apiVersion: v1
kind: Service
metadata:
  name: nginx-server
  namespace: default
spec:
  selector:
    app: web-app
  type: ClusterIP
  ports:
    - name: nginx-server-port
      protocol: TCP
      port: 80
      targetPort: 80
